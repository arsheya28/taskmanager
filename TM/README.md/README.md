# Dockerized Task Manager with Vector Search

## Features
- React frontend with localStorage caching
- Flask backend with PostgreSQL
- pgvector vector similarity search
- Dockerized for easy deployment

## Run Locally

```bash
docker-compose up --build
